function applyGlobalAccessibility(){let s={textSize:'medium',contrast:'normal',keyboardNav:false};try{s={...s,...JSON.parse(localStorage.getItem('petcare_accessibility')||'{}')}}catch{};const size=['small','medium','large'].includes(String(s.textSize).toLowerCase())?String(s.textSize).toLowerCase():'medium';document.body.classList.remove('text-size-small','text-size-medium','text-size-large','high-contrast','keyboard-navigation-active');document.body.classList.add('text-size-'+size);if(s.contrast==='high')document.body.classList.add('high-contrast');if(s.keyboardNav)document.body.classList.add('keyboard-navigation-active');}
document.addEventListener('DOMContentLoaded',applyGlobalAccessibility);